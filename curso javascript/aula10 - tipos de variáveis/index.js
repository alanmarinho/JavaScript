// String
const nome = 'Alan';
const nome2 = "Alan";
const nome3 = `Alan`;

// Number
const numero = 1;
const numero2 = 1.5;

// Undefined = não aponta par alugar nenhum na memória
let variable;

// Null = não aponta par alugar nenhum na memória
const variable2 = null;

// Boleano = só possui duas possibilidades de valor, true ou false
const boleano = true;
const boleano2 = false;

// se usar a variável duas vezes no typeof, dá para ver o tipo dela e o valor
console.log(typeof nome, nome); // --> string Alan